#!/bin/bash

# Get the name of the current branch
branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "❌ Error: Direct commits to the 'main' branch are prohibited."
  echo "Please switch to a feature branch to make your changes."
  exit 1
fi

exit 0